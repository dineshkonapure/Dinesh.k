// Minimal, safe preload. Not exposing any node API to renderer.
// If you need a small secure bridge, add whitelisted IPC methods here.
(function() {
  // Intentionally empty — using default browser APIs (localStorage / fetch)
})();
